(function () {
    $('.items-list').append('<%=escape_javascript  render 'share/items_list', :items_group => @items_group %>');
    if ($('.items-list .item').length >=<%=@total||0 %>) {
        $('.more-btn').text('No more~').data('no-more', true);
    }
}());
